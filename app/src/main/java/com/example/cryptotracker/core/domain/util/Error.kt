package com.example.cryptotracker.core.domain.util

/**
 * Base interface for error types in the application.
 * Used as a marker interface for type-safe error handling across different layers.
 */
interface Error